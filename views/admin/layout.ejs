<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Admin MOOTERACT HUB</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
    }
  </style>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">
  <header class="bg-blue-600 text-white shadow-md">
    <div class="container mx-auto px-4 py-4">
      <div class="flex justify-between items-center">
        <h1 class="text-2xl font-bold">MOOTERACT HUB Admin</h1>
        <div>
          <a href="/admin/logout" class="text-white hover:text-blue-200 transition">Logout</a>
        </div>
      </div>
    </div>
  </header>

  <div class="flex flex-1">
    <!-- Sidebar -->
    <aside class="w-64 bg-white shadow-md">
      <nav class="p-4">
        <ul class="space-y-2">
          <li>
            <a href="/admin/dashboard" class="block p-2 rounded-lg <%= active === 'dashboard' ? 'bg-blue-100 text-blue-600' : 'hover:bg-gray-100' %> transition">
              Dashboard
            </a>
          </li>
          <li>
            <a href="/admin/promotions" class="block p-2 rounded-lg <%= active === 'promotions' ? 'bg-blue-100 text-blue-600' : 'hover:bg-gray-100' %> transition">
              Kelola Promosi
            </a>
          </li>
          <li>
            <a href="/admin/n8n-requests" class="block p-2 rounded-lg <%= active === 'n8n-requests' ? 'bg-blue-100 text-blue-600' : 'hover:bg-gray-100' %> transition">
              Permintaan N8n
            </a>
          </li>
          <li>
            <a href="/admin/users" class="block p-2 rounded-lg <%= active === 'users' ? 'bg-blue-100 text-blue-600' : 'hover:bg-gray-100' %> transition">
              Pengguna
            </a>
          </li>
          <li>
            <a href="/admin/requests" class="block p-2 rounded-lg <%= active === 'requests' ? 'bg-blue-100 text-blue-600' : 'hover:bg-gray-100' %> transition">
              JSON Requests
            </a>
          </li>
          <li>
            <a href="/admin/server-templates" class="block p-2 rounded-lg <%= active === 'server-templates' ? 'bg-blue-100 text-blue-600' : 'hover:bg-gray-100' %> transition">
              Template Server
            </a>
          </li>
          <li>
            <a href="/admin/config" class="block p-2 rounded-lg <%= active === 'config' ? 'bg-blue-100 text-blue-600' : 'hover:bg-gray-100' %> transition">
              Konfigurasi
            </a>
          </li>
        </ul>
      </nav>
    </aside>

    <!-- Main Content -->
    <main class="flex-1 p-6">
      <div class="mb-6">
        <h2 class="text-2xl font-bold text-gray-800"><%= title %></h2>
      </div>

      <% if (success) { %>
        <div class="mb-4 p-3 bg-green-100 text-green-700 rounded-lg">
          <%= success %>
        </div>
      <% } %>

      <% if (error) { %>
        <div class="mb-4 p-3 bg-red-100 text-red-700 rounded-lg">
          <%= error %>
        </div>
      <% } %>

      <%- body %>
    </main>
  </div>

  <footer class="bg-white py-4 border-t">
    <div class="container mx-auto px-4 text-center text-gray-600">
      <p>&copy; <%= new Date().getFullYear() %> MOOTERACT HUB. All rights reserved.</p>
    </div>
  </footer>
</body>
</html>